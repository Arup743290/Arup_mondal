# HTML Validator Check

## Validation target
- `index.html`
- `pages/data.html`
- `pages/forms.html`

## Expected proof
Run the W3C Nu HTML Checker (or W3C Markup Validation Service) against the deployed/local HTML pages and record the result here.

**Current repository implementation:** semantic HTML5 landmarks, labeled form controls, table captions and scoped headers, skip links, visible focus styles, and a native accessible dialog are included.

**Validation status:** Ready for W3C validation. The repository does not claim zero validator errors until the validator has been run against the final deployed pages.
