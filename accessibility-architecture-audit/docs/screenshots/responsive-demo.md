# Responsive Demo Screenshot Evidence

Capture the dashboard at these required viewport widths and save the screenshots in this folder:

- `320px` — mobile portrait
- `768px` — tablet
- `1024px` — desktop
- `1440px` — large desktop

Suggested filenames:

```text
responsive-320.png
responsive-768.png
responsive-1024.png
responsive-1440.png
```

## Mobile overflow check

At 320px, confirm that the page itself has no horizontal scrollbar. The table is intentionally contained inside `.table-wrapper` so only the table region can scroll horizontally when its content cannot fit.

## Theme proof

Also capture one light-theme and one dark-theme screenshot if demonstrating `[data-theme="dark"]`.

> These are evidence placeholders. The screenshots must be captured from the rendered page to claim visual proof; this repository does not claim screenshots were captured automatically.
